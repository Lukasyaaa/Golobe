import React, { FC } from "react";
import { useTypedSelector } from "../../useTypedSelector";

export const Intro : FC = () =>{
    return(
        <section className="start__intro intro-start intro"></section>
    )
}